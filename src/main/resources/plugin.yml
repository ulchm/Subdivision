main: ${project.main}
author: ${project.authors}
version: ${project.version}
name: ${project.name}
softdepend: ${project.softdepend}
depend: ${project.depend}

commands:
  region:
    aliases: [sd]
    description: root subdivision command
  debug:
    aliases: [dbg]

